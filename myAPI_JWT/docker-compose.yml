#se pueden orquestar 2 o mas contenedores,
#cada uno con su propia imagen, y se pueden comunicar entre ellos para un mismo proyecto

services:
  api:
    build: .
    container_name: server_FastAPI2
    ports:
      - "5001:5001"       
    volumes:
      - ./app:/app/app
